import 'package:flutter/material.dart';

class HashtagText extends StatelessWidget {
  const HashtagText({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
